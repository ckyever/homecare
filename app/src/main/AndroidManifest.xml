<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sayyaf.homecare">

    <!-- permissions needed for webRTC realtime chat -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
    -->


    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <application
        android:name=".notifications.NotificationChannels"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".accounts.LaunchActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".contacts.ContactUpdateActivity">
        </activity>
        <activity android:name=".requests.RequestActivity">
        </activity>
        <activity android:name=".communication.CallScreenActivity">
        </activity>
        <activity android:name=".communication.IncomingVoiceCallActivity">
        </activity>
        <activity
            android:name=".contacts.ContactChatActivity"
            android:windowSoftInputMode="stateHidden">
        </activity>
        <activity
            android:name=".communication.ChatActivity"
            android:windowSoftInputMode="stateHidden">
        </activity>
        <activity android:name=".communication.VoiceCallActivity">
        </activity>
        <activity android:name=".accounts.LoginActivity">
        </activity>
        <activity android:name=".accounts.AccountRegisterActivity" />
        <activity
            android:name=".mapping.MapsActivity"
            android:label="@string/title_activity_maps"/>

        <activity android:name=".notifications.EmergencyCallActivity"/>
        <activity android:name=".options.OptionActivity" />
        <activity android:name=".options.ProfileImageActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <service
            android:name=".mapping.TrackingService"
            android:enabled="true"
            android:exported="true" />

        <service
            android:name=".notifications.NotificationService"
            android:enabled="true"
            android:exported="true" />

        <service
            android:name=".notifications.EmergencyMsgListener"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".mapping.TrackingActivity"
            android:label="@string/title_activity_tracking"></activity>
        <service
            android:name=".communication.SinchService"
            android:enabled="true"/>
    </application>

</manifest><!--
     <application
        <service android:name=".NotificationService" />
     </application>
-->
